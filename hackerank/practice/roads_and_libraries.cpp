#include<iostream>
#include<algorithm>
#include<vector>
#include<queue>
#include<list>
#include<pair>

use namespace std;

int main(){
	
	int q;
	cin>>q;
	while(q--){
		int n,m,clib,croad;
		cin>>n>>m>>clib>>croad;
		vector<int> comps;
		vector<bool> visited(n);
		vector<list<int> > graph(n);
		queue<int> q;
		
		for(int i=0;i<m;i++){
			int u,v;
			

		}
	}
}
